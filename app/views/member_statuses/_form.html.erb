<%= form_with(model: member_status, local: true) do |form| %>
  <% if member_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member_status.errors.count, "error") %> prohibited this member_status from being saved:</h2>

      <ul>
        <% member_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status, class: "required" %>
    <%= form.text_field :status, :size=>"16" %>
  </div>

  <div class="field">
    <%= form.label :description, class: "required" %>
    <%= form.text_field :description, :size=>"100" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
