<%= form_with(model: officer_position, local: true) do |form| %>
  <% if officer_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(officer_position.errors.count, "error") %> prohibited this officer_position from being saved:</h2>

      <ul>
        <% officer_position.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :position, class: "required" %>
    <%= form.text_field :position, :size=>"25" %>
  </div>

  <div class="field">
    <%= form.label :description, class: "required" %>
    <%= form.text_field :description, :size=>"100" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
