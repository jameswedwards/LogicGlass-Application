<%= form_with(model: coordinator_status, local: true) do |form| %>
  <% if coordinator_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coordinator_status.errors.count, "error") %> prohibited this coordinator_status from being saved:</h2>

      <ul>
        <% coordinator_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status, class: "required" %>
    <%= form.text_field :status, :size=>"16" %>
  </div>

  <div class="field">
    <%= form.label :description, class: "required" %>
    <%= form.text_field :description, :size=>"100" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
